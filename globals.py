# for Position field: position[X] / position[Y]
X = 0
Y = 1

# Difficulty Section
Default_HP = float(5)
Default_CS = float(5)
Default_OD = float(5)
Default_AR = float(5)
Default_SliderMult = 1.4
Default_SliderTickRate = 1.0